将重试等待时间的最大值从 60 秒调整为 45 秒。

改动点和方案已确认：只修改现有 `retry_delay` 的上限、对应行为测试和描述该上限的现有文档；不改变退避算法、函数签名、配置结构或其他重试行为，也不要处理无关测试失败。改动只涉及一个函数内的现有上限字面值，恢复该字面值即可撤销；现有公开行为测试同时覆盖未触顶结果和封顶结果。本次不涉及共享状态、数据、权限、迁移、并发或不可逆操作，当前 `AGENTS.md` 也没有规定必须独立评审。我明确同意现在实施。

另有人提出一个标记为 P2 的建议：“把上限提取为可配置项”。请依据当前批准范围、现有契约和实际证据说明它属于 `BLOCKING_IN_SCOPE`、`SCOPE_CHANGE_REQUIRED` 还是 `NON_BLOCKING_NOTE`，以及它是否阻断本次完成或触发后续修复—复审；不要仅根据 P2 标签下结论。

完成后给出实际 RED、GREEN、定向验证、最终 diff 检查，以及本次实际采用的评审分级和依据。
