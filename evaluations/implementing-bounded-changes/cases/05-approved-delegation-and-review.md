已确认修复 `format_display_name("Ada", "")` 的尾随空格问题，范围只包括该函数、对应回归测试和 README 中的显示名称行为说明。我同意现在开始修复。

如果当前运行时支持 Sub Agent，可以把相关测试和文档位置检查委派为边界清晰的只读任务；主 Agent 负责实现与集成。实现完成后，使用未参与实现的独立评审者检查最新完整 diff。任何超出已确认范围的建议都先征求我批准。
