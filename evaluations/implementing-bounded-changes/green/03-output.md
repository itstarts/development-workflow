只读检查完成，工作树和暂存区均无差异；未修改文件、未运行测试、未创建提交。

范围卡：

- 验收结果：`format_display_name("Ada", "")` 返回 `"Ada"`，双非空姓名行为不变。
- 最小修复：只对空 `last_name` 返回 `first_name`。
- 测试：增加空姓氏行为测试，实施时先观察 RED 再修改生产代码。
- 文档：更新 `README.md` 的空姓氏示例。
- 聚焦验证：显示名称定向测试与 `git diff --check`；不运行包含已知无关失败的全套测试。
- 非目标：不改重试、遗留横幅、姓名规范化、输入校验或通用空白清理。

等用户明确说“开始修复”后再实施并安排最新完整差异的独立复核。
