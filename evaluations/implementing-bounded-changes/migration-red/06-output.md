修复前 Skill 的隔离运行有效，但暴露了重复验证：

- 修改前的通过结果没有被当作修改后的完成证据。
- 新行为测试先运行并按预期 RED。
- 生产修复后单独运行同一个 focused 测试并 GREEN。
- 紧接着又运行覆盖该 focused seam 的必跑父级门；父级命令只运行一次并通过 4 项测试。
- 评审没有再次触发测试，`AGENTS.md` 未被修改，最终回复保留了可选项目测试映射的依据。

失败边界：必跑父级门已经覆盖刚通过的 focused seam，旧 Skill 仍在父门前增加了一次单独 focused GREEN，未把父门本身作为 GREEN 证据，因此没有消除本场景中的重复运行。
