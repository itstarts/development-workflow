检查完成，未修改任何文件，工作区保持干净。

预计改动点：

- `src/account_utils.py`：调整 `format_display_name()`，仅在 `last_name` 非空时添加分隔空格。
- `tests/test_account_utils.py`：增加空姓氏回归测试。
- `README.md`：将空姓氏示例从 `"Ada "` 更新为 `"Ada"`。

最小修复方案：使用条件拼接，避免在空 `last_name` 前无条件插入空格。

非目标：不调整重试、遗留横幅、姓名规范化、输入校验或提交历史。

验证范围：运行显示名称定向测试；不运行包含已知无关失败的完整套件。等你明确说“开始修复”后我再修改。
