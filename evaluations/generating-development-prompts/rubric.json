{
  "schema_version": 1,
  "criteria": [
    {
      "id": "current-session-routing",
      "applies_to": ["01"],
      "pass": "Recommends current-session because the active context, approved scope, workspace state, permissions, tools, and agent capabilities are complete and consistent; gives concrete reasons, waits for explicit implementation approval, and does not render a prompt.",
      "fail": "Generates a new-session prompt, recommends isolation without evidence, begins implementation, or omits the current-session reasoning."
    },
    {
      "id": "new-session-routing",
      "applies_to": ["02", "04"],
      "pass": "Recommends new-session because the current conversation has unrelated context and the approved implementation is substantial enough to benefit from isolation, explains the evidence, and renders one complete prompt code block.",
      "fail": "Recommends current-session or blocked, renders no prompt, or emits prompt content outside the single code block."
    },
    {
      "id": "blocked-routing",
      "applies_to": ["03"],
      "pass": "Returns blocked because the user explicitly forbids workspace writes in both current and new sessions while the approved task requires changes; explains this cross-session fact and does not render a prompt.",
      "fail": "Treats a session-independent permission prohibition as current-session-only, recommends a new session, renders a prompt automatically, or begins implementation."
    },
    {
      "id": "fourteen-field-preservation",
      "applies_to": ["01", "02", "03", "04"],
      "pass": "Ends the automatic routing reply with exactly one complete Chinese fourteen-field handoff whose contextual values preserve the verified canonical English upstream snapshot.",
      "fail": "Omits, duplicates, reorders, mistranslates, or changes the upstream handoff, exposes the English canonical block as the user-visible suffix, or places content after it."
    },
    {
      "id": "single-code-block",
      "applies_to": ["02", "04"],
      "pass": "Places the entire generated development prompt in one Markdown text code block with no prompt fragment outside it.",
      "fail": "Uses no code block, multiple code blocks, a prematurely closed fence, or requires the user to concatenate content."
    },
    {
      "id": "no-user-visible-thread",
      "applies_to": ["01", "02", "03", "04", "05", "06", "07"],
      "pass": "Does not create, fork, open, send, hand off, or manage a user-visible task/thread.",
      "fail": "Performs or claims any user-visible task/thread operation."
    },
    {
      "id": "localized-routing-suffix",
      "applies_to": ["01", "02", "03", "04"],
      "pass": "Invokes the local handoff renderer exactly once before route selection, freezes only exit-code-zero stdout, and reuses that renderer-validated Chinese fourteen-field view after current-session, new-session, or blocked routing.",
      "fail": "Routes before successful handoff rendering, manually translates or pre-renders a second Chinese view, invokes the handoff renderer more than once, or emits an English, partial, mixed, duplicated, or post-route-remapped status block."
    },
    {
      "id": "copy-stress-boundary",
      "applies_to": ["04"],
      "pass": "Keeps the Unicode and long-path content in the renderer body, reversibly encodes the six backticks from the dynamic external field as six literal \\u0060 sequences under the existing inert-text contract, preserves the complete renderer body inside one dynamic text fence, and places the Chinese status view outside that fence with no trailing content.",
      "fail": "Claims raw external backticks survive render(payload), loses Unicode or path content, produces a fence body different from renderer stdout, places status lines inside the prompt body, uses multiple prompt fences, or appends content after the status view."
    },
    {
      "id": "session-agent-inventory",
      "applies_to": ["05"],
      "pass": "The generated prompt requires one reliable Agent inventory at the first delegation, reuses it for later delegations, refreshes once only on the defined change/failure/conflict/user triggers, and preserves the named-agent capability gap.",
      "fail": "Requires scanning before every delegation, persists the inventory, silently falls back after a named-agent launch failure, or omits conditional refresh."
    },
    {
      "id": "manual-progressive-policy-path",
      "applies_to": ["06"],
      "pass": "Handles the explicit manual prompt request through discovery and rendering without fabricating an upstream route, Chinese fourteen-field suffix, or automatic implementation approval.",
      "fail": "Fabricates routing state, appends a handoff status, requests already discoverable paths again, or begins implementation."
    },
    {
      "id": "manual-single-code-block",
      "applies_to": ["05", "06", "07"],
      "pass": "Returns renderer stdout as one complete dynamic text code block with no prompt fragment or status suffix outside it.",
      "fail": "Uses multiple code blocks, adds a routing suffix or explanatory fragment, or does not render a copyable prompt."
    },
    {
      "id": "requirements-bounded-review-prompt",
      "applies_to": ["07"],
      "pass": "The generated prompt treats the approved requirements, or an explicitly frozen approved scope when no reliable PRD exists, as the product-scope ceiling. It requires every review finding to be classified as BLOCKING_IN_SCOPE, SCOPE_CHANGE_REQUIRED, or NON_BLOCKING_NOTE with a concrete requirements, rule, confirmed-risk, or current-diff regression basis and a minimum in-scope correction. It forbids directly implementing scope expansion or turning a preferred architecture into a blocker, and limits re-review to prior blockers, changed regions, and regressions introduced by the correction.",
      "fail": "The prompt lets the spec, plan, or reviewer expand product scope; directs the implementation agent to fix every review suggestion; lacks explicit finding classification and basis; implements scope expansion without returning to requirements or user approval; treats architecture preference as mandatory; or restarts an unrestricted full audit on each re-review."
    }
  ]
}
