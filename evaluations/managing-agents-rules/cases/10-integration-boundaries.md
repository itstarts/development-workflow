我已批准一个小型受控修改：把 README 中的重试等待上限从 60 秒改为 30 秒，先建立 RED、再最小实现、定向验证，并由独立 reviewer 检查最新完整 diff。与此同时按规则治理流程处理开发前检查和完成阶段候选，但不要调用其它 skill 作为依赖。任何项目 AGENTS.md 候选必须在最终评审前单独展示 diff 并取得批准，写后重新验证并让同一 reviewer 检查最新 diff。不得读取或操作任何规则源仓库、真实全局规则或真实 skill home。
