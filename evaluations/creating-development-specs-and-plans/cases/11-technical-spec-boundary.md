订单审批的产品范围是 feature，稳定主题是 `order-approval`，已批准 PRD 路径是 `docs/requirements/2026-07-12-order-approval.md`。

请据此只创建一份尚未评审的技术 spec 草案并立即交接，不执行自审或独立评审，审批状态保持 pending。采用框架无关的服务 API 和关系型事务语义，不绑定具体 Web 框架或数据库产品。该功能需要明确审批 API、审批记录与订单的实体关系、状态迁移、持久化迁移边界和并发一致性；在交接回复中附上生成的完整 spec 正文，使这些技术边界可独立检查。不要创建 plan，也不要实现代码。
