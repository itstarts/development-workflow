# Fictional order service

This fixture represents a local order service with approval statuses and CSV export. `src/orders.py` is an internal module, and no public API, data model, permission, migration, concurrency, production configuration, or external-state boundary is in scope.
