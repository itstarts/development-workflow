仓库目前只有订单创建逻辑，没有持久化或审批边界，无法据此推定一致性模型。

唯一需要先确认的问题是：当 `order-approval` 返回“审批成功”时，是否必须保证审批记录与订单状态已经同时提交，并且任何读取方都不能观察到只成功一侧？

- **A，必须**：采用强一致事务；任一写入失败则整体失败。
- **B，不必须**：采用最终一致流程；可先返回“已受理/处理中”，允许短暂不一致，再异步收敛。

判断标准：若“审批成功”会立即触发履约、扣款等不可容忍误判的动作，倾向 A；只有业务明确容忍中间态时才考虑 B。请确认 A 或 B；本轮未写任何文档。

当前阶段：技术规格澄清
主题：order-approval
下一步：请确认审批成功时是否要求审批记录与订单状态原子提交且同步可见。
