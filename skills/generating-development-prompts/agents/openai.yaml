interface:
  display_name: "Route Development Sessions"
  short_description: "Route approved handoffs and generate copyable prompts"
  default_prompt: "Use $generating-development-prompts to route this approved handoff and generate a copyable prompt only when a new session is appropriate."
